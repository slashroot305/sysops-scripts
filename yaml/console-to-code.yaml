CFN/YAML

---
AWSTemplateFormatVersion: '2010-09-09'
Description: 'AWS CloudFormation template to create resources based on provided CLI commands'

Resources:

  LaunchWizard1SecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupName: launch-wizard-1
      GroupDescription: launch-wizard-1 created 2026-02-09T02:47:09.776Z
      VpcId: vpc-0079cd8475d12fba3
      SecurityGroupIngress:
        - IpProtocol: tcp
          FromPort: 22
          ToPort: 22
          CidrIp: 0.0.0.0/0

  EC2Instance:
    Type: AWS::EC2::Instance
    Properties:
      ImageId: ami-03ea746da1a2e36e7
      InstanceType: t2.micro
      NetworkInterfaces:
        - AssociatePublicIpAddress: true
          DeviceIndex: 0
          GroupSet:
            - !Ref LaunchWizard1SecurityGroup
      CreditSpecification:
        CPUCredits: standard
      MetadataOptions:
        HttpEndpoint: enabled
        HttpPutResponseHopLimit: 2
        HttpTokens: required
      PrivateDnsNameOptions:
        HostnameType: ip-name
        EnableResourceNameDnsARecord: true
        EnableResourceNameDnsAAAARecord: false

Reasoning:

1) Libraries and their versions used: AWS CloudFormation, AWS::EC2::SecurityGroup, AWS::EC2::Instance
2) AWS resources impacted: AWS::EC2::SecurityGroup, AWS::EC2::Instance
3) AWS actions performed: Create a security group, allow inbound SSH traffic, launch an EC2 instance with specified configurations
4) The generated YAML creates an AWS::EC2::SecurityGroup resource with the specified name, description, and VPC ID. It also allows inbound SSH traffic from any IP address. Additionally, it creates an AWS::EC2::Instance resource with the specified AMI, instance type, network interface configuration (including the security group), CPU credit specification, metadata options,d private DNS name options.